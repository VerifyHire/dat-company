tasks:
  - init: |
      # Install Go dependencies
      go mod tidy
  - command: |
      # Start the Go application server
      go run main.go

ports:
  - port: 8080
    onOpen: open-preview

vscode:
  extensions:
    - ms-go.vscode-go
